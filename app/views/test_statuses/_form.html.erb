<%= form_for(test_status) do |f| %>
  <% if test_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(test_status.errors.count, "error") %> prohibited this test_status from being saved:</h2>

      <ul>
      <% test_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
