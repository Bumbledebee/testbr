<%= form_for(result_status) do |f| %>
  <% if result_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(result_status.errors.count, "error") %> prohibited this result_status from being saved:</h2>

      <ul>
      <% result_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
